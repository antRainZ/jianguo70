### 建国70周年

+ 采用bootstrap3前端框架
+ 利用owl.carousel显示分页内容
+ 利用json文件存储数据
+ 利用jQuery
+ 无需服务器即可本地调试，前端数据来源于json文件

### 遇到的问题
```
from origin 'null' has been blocked by CORS policy: Cross origin requests are only supported for protocol schemes: http, data, chrome, chrome-extension, https.send @ jquery.min.js:3171
```

>解决:利用js回调